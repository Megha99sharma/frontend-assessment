{
  "name": "frontend",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx --fix"
  },
  "dependencies": {
    "axios": "^1.3.0", // HTTP client to make API requests
    "react": "^18.2.0", // React library
    "react-dom": "^18.2.0", // React DOM for rendering components
    "react-scripts": "^5.0.1", // Create React App build scripts
    "react-table": "^7.8.0", // Library for displaying data in tables
    "eslint": "^8.0.0", // Linter for JavaScript/React code
    "eslint-plugin-react": "^7.31.0", // ESLint plugin for React
    "axios-mock-adapter": "^1.20.0" // Mocking library for Axios (optional, for testing)
  },
  "devDependencies": {
    "typescript": "^4.5.5", // TypeScript for type checking and development
    "eslint-plugin-import": "^2.25.3", // ESLint plugin for import rules
    "eslint-plugin-jsx-a11y": "^6.5.1", // ESLint plugin for accessibility checks in JSX
    "eslint-plugin-react-hooks": "^4.6.0" // ESLint plugin for React hooks linting
  }
}
